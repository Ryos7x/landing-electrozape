---
import { Image } from "astro:assets";
---

<section class="section-container bg-darkBackground text-white">
  <div class="flex flex-col max-w-section">
    <div
      class="grid grid-cols-1 gap-y-12 gap-x-12 text-base md:text-lg lg:gap-y-24 lg:grid-cols-2 text-pretty !leading-relaxed"
    >
      <div class="flex flex-col items-center gap-y-6">
        <h2 class="text-2xl font-bold">Contacto</h2>
        <p>¡Contactanos por correo!</p>
        <input
          type="email"
          name=""
          id=""
          placeholder="tucorreo@gmail.com"
          class="w-full px-6 py-2 rounded-xl text-textColor"
        />
        <button
          type="submit"
          class="w-1/2 p-2 rounded-xl font-bold text-textColor bg-white max-w-36 sm:max-w-44 md:max-w-48"
          >Enviar</button
        >
        <div class="flex flex-row gap-x-6">
          <div class="bg-primaryRed w-1/2 px-6 py-4 rounded-xl">.</div>
          <div class="bg-primaryRed w-1/2 px-6 py-4 rounded-xl">.</div>
          <div class="bg-primaryRed w-1/2 px-6 py-4 rounded-xl">.</div>
        </div>
      </div>
    </div>
  </div>
</section>
